const path = require('path');


module.exports = {
  "config": path.resolve('./app/config/', 'database.js'),
  "models-path": path.resolve('./app/db/models'), //migrations folder will hold our application's migrations
  "seeders-path": path.resolve('./app/db/seeders'), //Seed data is initial data provided with a system for testing, training or templating purposes. The seeders folder typically holds seed data, but we're not going to be using that in this tutorial.
  "migrations-path": path.resolve('./app/db/migrations')
};
